apiVersion: apps/v1
kind: Deployment
metadata:
  name: selector-demo-app
  labels:
    app: selector-demo-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: selector-demo-app
  template:
    metadata:
      labels:
        app: selector-demo-app
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
