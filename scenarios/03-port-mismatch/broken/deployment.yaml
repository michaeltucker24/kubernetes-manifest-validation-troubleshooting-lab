apiVersion: apps/v1
kind: Deployment
metadata:
  name: port-demo-app
  labels:
    app: port-demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: port-demo-app
  template:
    metadata:
      labels:
        app: port-demo-app
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
